# Contributing

Keep examples synthetic and public CI credential-free. Run Terraform formatting, backend-disabled initialization, validation and mocked tests described in README.md. Describe behavior changes and update the migration notes when input contracts change.

Do not submit real state, plans, private tfvars, exported cloud configurations or certificates.
