# Security policy

Do not publish tenant estate configuration, certificates, state, saved plans or credentials in issues or pull requests. Report a suspected vulnerability privately through the repository host's security reporting facility when available.

Use a private deployment copy for real subscription IDs, hostnames, backend targets and policy exceptions. Public CI must remain credential-free. WAF exceptions and application behavior require review against your own service before deployment.
